# Migration `20200413195156-added-blog`

This migration has been generated by Carter Rabasa at 4/13/2020, 7:51:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Post" (
    "author" text  NOT NULL ,
    "body" text  NOT NULL ,
    "id" SERIAL,
    "image" text   ,
    "postedAt" timestamp(3)   ,
    "slug" text  NOT NULL ,
    "title" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Tag" (
    "id" SERIAL,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."User" (
    "email" text  NOT NULL ,
    "id" SERIAL,
    "isAdmin" boolean  NOT NULL DEFAULT false,
    "name" text   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."_PostToTag" (
    "A" integer  NOT NULL ,
    "B" integer  NOT NULL 
) 

CREATE UNIQUE INDEX "Post.slug" ON "public"."Post"("slug")

CREATE UNIQUE INDEX "Tag.name" ON "public"."Tag"("name")

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "_PostToTag_AB_unique" ON "public"."_PostToTag"("A","B")

CREATE  INDEX "_PostToTag_B_index" ON "public"."_PostToTag"("B")

ALTER TABLE "public"."_PostToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."Post"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_PostToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200413133929-init..20200413195156-added-blog
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -10,5 +10,29 @@
 model World {
   id         Int    @id @default(autoincrement())
   name       String @unique
   population Float
+}
+
+model Post {
+  id Int @id @default(autoincrement())
+  title String
+  slug String @unique
+  author String
+  body String
+  image String?
+  tags Tag[]
+  postedAt DateTime?
+}
+
+model Tag {
+  id Int @id @default(autoincrement())
+  name String @unique
+  posts Post[]
+}
+
+model User {
+  id Int @id @default(autoincrement())
+  name String?
+  email String @unique
+  isAdmin Boolean @default(false)
 }
```


